import { ArrowLeft, ArrowRight, Check, CircleHelp, FileUp, MapPin, Sparkles, WandSparkles } from "lucide-react";
import { Link } from "wouter";
import { useState } from "react";

const steps = ["Tell us", "Understand", "Add details", "Review"];
export default function CreateNeed() {
  const [step, setStep] = useState(0);
  const [submitted, setSubmitted] = useState(false);
  const [text, setText] = useState("");
  const next = () => setStep(Math.min(3, step + 1));
  return <div className="app-page create-page"><header className="app-header container"><Link href="/" className="brand"><span className="brand-mark">m</span><span>msaada</span></Link><span className="draft-status">Draft saved just now</span><Link href="/discover" className="exit-link">Exit <ArrowLeft size={14} /></Link></header><main className="create-main container">{submitted ? <div className="success-state"><div className="success-mark"><Check size={28} /></div><span className="section-kicker">Submitted for review</span><h1>Your need is now<br /><em>in good hands.</em></h1><p>A Msaada moderator will review the details before the request becomes public. We will let you know when there is an update.</p><Link href="/dashboard" className="button button-dark">Go to my dashboard <ArrowRight size={17} /></Link></div> : <><div className="stepper">{steps.map((item, i) => <div className={`step ${i === step ? "active" : i < step ? "complete" : ""}`} key={item}><span>{i < step ? <Check size={13} /> : i + 1}</span><label>{item}</label></div>)}</div><div className="create-card">{step === 0 && <div className="create-step"><div className="step-heading"><span className="section-kicker">Step 1 / Tell us what is needed</span><h1>Start with the story.</h1><p>Write naturally. You do not need to have all the details yet.</p></div><label className="field-label" htmlFor="need-story">What does your community need, and why?</label><textarea id="need-story" value={text} onChange={(e) => setText(e.target.value)} placeholder="Tell us what your community needs and why. For example: Our school has 42 students and most of them don't have desks..." rows={7} /><div className="field-hint"><CircleHelp size={15} /> Your words stay editable. Msaada will never publish AI suggestions without your review.</div></div>}{step === 1 && <div className="create-step"><div className="step-heading"><span className="section-kicker">Step 2 / Here’s what we understood</span><h1>A clearer shape<br /><em>for your request.</em></h1><p>AI-assisted suggestions based on your story. Review and edit every field before continuing.</p></div><div className="ai-notice"><WandSparkles size={18} /><span><strong>AI-assisted</strong> These are suggestions, not verification. We found a few useful starting points.</span></div><div className="structured-grid"><Field label="Category" value="Education" /><Field label="Need title" value="Desks for community school students" /><Field label="Urgency" value="High" pill /><Field label="Estimated quantity" value="20 desks" /><Field label="Beneficiaries" value="42 students" /><Field label="Contribution types" value="Items · Money · Transport" /></div><div className="missing-info"><Sparkles size={16} /><div><strong>Nothing important seems missing.</strong><span>You can add more context in the next step.</span></div></div></div>}{step === 2 && <div className="create-step"><div className="step-heading"><span className="section-kicker">Step 3 / Add a few details</span><h1>Help people understand<br /><em>where to show up.</em></h1><p>These details help contributors make informed choices.</p></div><div className="structured-grid"><Field label="Location" value="Kiambu" icon={<MapPin size={15} />} /><Field label="Beneficiaries" value="42" /><Field label="Quantity / goal" value="20 desks" /><Field label="Category" value="Education" /><Field label="Urgency" value="High" pill /><Field label="Help welcome" value="Money · Items · Skills · Logistics" /></div><label className="upload-zone"><FileUp size={20} /><span><strong>Attach supporting information</strong><small>Optional · images or documents · file references only</small></span><input type="file" accept="image/*,.pdf,.doc,.docx" /></label></div>}{step === 3 && <div className="create-step"><div className="step-heading"><span className="section-kicker">Step 4 / Review before sharing</span><h1>This is what will<br /><em>become public.</em></h1><p>Take a final look. It will be marked “Pending review” after you submit.</p></div><div className="review-preview"><div className="review-label"><span className="badge badge-cream">Pending review</span><span className="review-demo">{"Fictional Kenyan community demo"}</span></div><h2>Desks for community school students</h2><p>Our school has 42 students and most of them don't have desks. We need about 20 desks before the next school term.</p><div className="review-details"><span>Education</span><span>Kiambu</span><span>42 beneficiaries</span><span>High urgency</span></div></div></div>}<div className="create-footer"><button className="text-button" onClick={() => step > 0 ? setStep(step - 1) : null}>{step > 0 && <><ArrowLeft size={15} /> Back</>}</button><div><button className="button button-ghost" onClick={() => {}}>Save draft</button>{step < 3 ? <button className="button button-dark" onClick={next}>Continue <ArrowRight size={16} /></button> : <button className="button button-dark" onClick={() => setSubmitted(true)}>Submit for review <ArrowRight size={16} /></button>}</div></div></div></>}</main></div>;
}
function Field({ label, value, pill, icon }: { label: string; value: string; pill?: boolean; icon?: React.ReactNode }) { return <label className="field"><span>{label}</span><div className={`field-input ${pill ? "pill-field" : ""}`}>{icon}{value}</div></label>; }
