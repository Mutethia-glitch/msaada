import { ArrowRight, Bell, Check, ChevronRight, Clock3, HeartHandshake, Leaf, Plus, Settings, ShieldCheck, Sparkles, Users } from "lucide-react";
import { Link } from "wouter";
import { activity, demoLabel, formatKsh, needs } from "@/lib/demoData";
export default function Dashboard() { return <div className="app-page"><header className="app-header container"><Link href="/" className="brand"><span className="brand-mark">m</span><span>msaada</span></Link><nav><Link href="/discover">Discover needs</Link><Link href="/create">Post a need</Link><Link href="/dashboard" className="active">My dashboard</Link></nav><div className="header-user"><button aria-label="Notifications"><Bell size={18} /></button><span className="avatar-link">JD</span></div></header><main className="container dashboard-main"><div className="dashboard-welcome"><div><span className="section-kicker">Tuesday, 02 June 2026</span><h1>Good morning, <em>Jane.</em></h1><p>Here is the difference you are helping make.</p></div><Link href="/create" className="button button-dark"><Plus size={17} /> Post a need</Link></div><div className="dashboard-stats"><Stat icon={<HeartHandshake />} label="Total pledged" value="KSh 18,500" tone="terracotta" /><Stat icon={<Check />} label="Needs fulfilled" value="3" tone="sage" /><Stat icon={<Clock3 />} label="Volunteer hours" value="12h" tone="ochre" /><Stat icon={<Users />} label="People reached" value="86" tone="lavender" /></div><div className="dashboard-grid"><section className="dashboard-panel"><div className="panel-heading"><div><span className="section-kicker">Your giving</span><h2>Contributions</h2></div><Link href="/discover" className="text-link">Find more ways <ArrowRight size={15} /></Link></div><div className="contribution-row"><div className="contribution-icon terracotta-bg"><HeartHandshake size={18} /></div><div className="contribution-detail"><strong>3 desks for Kijiji Community School</strong><span>Items · pledged 28 May 2026</span></div><span className="status-tag status-pledged">Pledged</span><ChevronRight size={16} /></div><div className="contribution-row"><div className="contribution-icon sage-bg"><Leaf size={18} /></div><div className="contribution-detail"><strong>Native tree planting day in Rongai</strong><span>Time · completed 31 May 2026</span></div><span className="status-tag status-complete">Completed</span><ChevronRight size={16} /></div><div className="contribution-row"><div className="contribution-icon ochre-bg"><Sparkles size={18} /></div><div className="contribution-detail"><strong>School supplies for Mwiki learners</strong><span>Money · pledged 18 May 2026</span></div><span className="status-tag status-confirmed">Confirmed</span><ChevronRight size={16} /></div></section><section className="dashboard-panel activity-panel"><div className="panel-heading"><div><span className="section-kicker">What is moving</span><h2>Recent activity</h2></div><button className="icon-button" aria-label="Activity settings"><Settings size={17} /></button></div>{activity.map((item) => <div className="activity-row" key={item.detail}><span className={`activity-dot ${item.tone}`} /><div><strong>{item.label}</strong><p>{item.detail}</p><small>{item.time}</small></div></div>)}</section></div><section className="dashboard-panel my-needs-panel"><div className="panel-heading"><div><span className="section-kicker">Your requests</span><h2>Needs you have shared</h2></div><Link href="/create" className="text-link"><Plus size={15} /> New need</Link></div><div className="mini-need-list">{needs.slice(0, 2).map((need) => <Link href={`/needs/${need.id}`} className="mini-need" key={need.id}><img src={need.image} alt="" /><div><span className="section-kicker">{need.category} · {need.location}</span><strong>{need.title}</strong><div className="mini-progress"><span style={{ width: `${need.progress}%` }} /></div><small>{need.progress}% fulfilled · {need.contributors} contributors</small></div><ChevronRight size={17} /></Link>)}</div></section><div className="dashboard-note"><ShieldCheck size={18} /><span><strong>A note on trust:</strong> Msaada is a prototype platform. All content shown here is fictional Kenyan community demo data.</span></div></main></div> }
function Stat({ icon, label, value, tone }: { icon: React.ReactNode; label: string; value: string; tone: string }) { return <div className="stat-card"><div className={`stat-icon ${tone}`}>{icon}</div><span>{label}</span><strong>{value}</strong></div>; }
